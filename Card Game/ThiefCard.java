public class ThiefCard extends Card {

    public ThiefCard() {
        // Thief card settings
        int pointValue = -1; // costs points to use

        super(pointValue);
    }

    @Override
    public void play(Player currentPlayer) {

    }
}
